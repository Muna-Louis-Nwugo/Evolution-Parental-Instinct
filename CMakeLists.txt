cmake_minimum_required(VERSION 3.15)
project(PhysSim LANGUAGES CXX)

# find_package(pybind11 REQUIRED)
# pybind11_add_module([module naame] src/main.cpp)

# For now, just a message to prove CMake is working
message(STATUS "C++ build is currently disabled. Ready for future expansion.")